name: Mon premier workflow

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  afficher-message:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "📝 Dernier commit :" && git log -1 --pretty=%B
